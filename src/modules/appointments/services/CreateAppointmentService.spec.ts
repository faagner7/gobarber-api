import CreateAppointmentService from './CreateAppointmentSevice';

describe('CreateAppointment', () => {
  it('should be able to create a new appoinment', () => {
    expect(1 + 2).toBe(3);
  });

  // it('should not be able to create two appointments on the same time', () => {
  //   expect(1 + 2).toBe(3);
  // });
});
